---
interface Props {
  text?: string;
}

const { text = "↓" } = Astro.props;
---

<div class="relative -mt-20">
  <div class="absolute inset-0 -mt-3 flex items-center" aria-hidden="true">
    <div class="w-full border-t border-blob-2"></div>
  </div>
  <div class="relative flex justify-center">
    <span
      class="-mt-3 rounded-full bg-blob-2/30 px-4 py-1 text-sm backdrop-blur-sm"
    >
      {text}
    </span>
  </div>
</div>
