---
interface Props {
  text?: string;
}

const { text = "↓" } = Astro.props;
---

<div class="relative -mt-[82px] -mb-[14px]">
  <div class="absolute inset-0 -mt-3 flex items-center" aria-hidden="true">
    <div class="w-full border-t border-blob-2"></div>
  </div>
  <div class="relative flex justify-center">
    <span
      class="-mt-3 rounded-full bg-blob-2 px-4 py-1 text-sm ring-1 ring-blob-2 backdrop-blur-sm z-50"
    >
      {text}
    </span>
  </div>
</div>
