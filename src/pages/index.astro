---
import { reader } from "src/reader";
import Layout from "src/layouts/Layout.astro";

const site = await reader.singletons.site.read();
---

<Layout>
  <h1 class="text-3xl font-bold underline">{site?.title}</h1>
</Layout>
